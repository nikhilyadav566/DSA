// Vector = > It is a such type of array which have not fixed size. 
//                it's a dynamic array

//    Declaration:-
//         ↓ 
//   vector<int> arr;         here vector is keyword,<int> -> datatype, arr -> variable 

//     Initialization:-
//          ↓
//    vector<int> arr(10,20,30);
//          OR
//    vector<int> arr(10, -1);

//    suppose i want to create 'n' size of vectore by taking input at runtime and create array.
//  like this:-
//              int n;
//              cin>>n;
//              vector<int> arr(n);

//   How insert value (element):-
//        there is push-back function by which we can insert
//        arr.push_back(5); 
//        arr.push_back(7);

//   How remove value (element):-
//      there is pop-back() function 
//      arr.pop_back() -> at the end element will be remove

//      To find the size of vector:-
//       arr.size();

//     To find the array is empty or not 
//       arr.empty();

// Similarly there are many function used in vector

//   To know more about the vector study these two websites

//  cppreference.com
//  cplusplus.com


// Note:-> The default size of vector is 0